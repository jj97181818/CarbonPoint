<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.jj97181818.carbonpoint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FAQRestaurantActivity" />
        <activity android:name=".FAQGameActivity" />
        <activity android:name=".MessageBoardActivity" />
        <activity android:name=".PointActivity" />
        <activity android:name=".ThisMonthRecordActivity" />
        <activity android:name=".HistoryRecordActivity" />
        <activity android:name=".NoticeActivity" />
        <activity android:name=".MemberDataActivity"></activity>
    </application>

</manifest>